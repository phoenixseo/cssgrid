<?php
/**
 * @file
 * CSSgrid module help and theme functions.
 */
// Theme functions in a separate .inc file.
\Drupal::moduleHandler()->loadInclude('cssgrid', 'inc', 'cssgrid.theme');

/**
 * Implements hook_theme().
 */
function cssgrid_theme($existing, $type, $theme, $path) {

  return array(
    'cssgrid' => array(
      'file' => 'cssgrid.theme.inc',
    ),
  );
}
